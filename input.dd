fun main() {
    var x = 5;
    var y = x + 3;
    var z = x > y;     // Valid: Comparison returns bool
    x = y * 2;
    if (z) {
        var w = 10;
        return w;
    } else {
        return x + y;
    }
    foo();             // Valid: Calls declared function
}

fun foo() {
    var a = 1;
    return a;
}